# Reference only. Real values live in the Vercel dashboard, never in this repo.
#
# Use the Neon POOLED host (with "-pooler") and the read-only stacklight_web
# role, not the owner role.

DATABASE_URL=postgresql://stacklight_web:<password>@<neon-pooled-host>/neondb?sslmode=require
