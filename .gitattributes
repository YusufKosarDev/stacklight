* text=auto eol=lf

.githooks/* text eol=lf
*.sh        text eol=lf
mvnw        text eol=lf
Dockerfile  text eol=lf

mvnw.cmd    text eol=crlf
*.cmd       text eol=crlf
*.bat       text eol=crlf

*.jar       binary
*.png       binary
*.ico       binary
# The recordings in docs/media. `* text=auto` already detects these correctly, so
# naming them changes nothing today -- it states the fact rather than leaving it to
# a heuristic, which is what the three lines above are for too.
*.gif       binary
*.webm      binary
