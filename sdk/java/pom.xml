<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<groupId>dev.stacklight</groupId>
	<artifactId>stacklight-sdk-parent</artifactId>
	<version>0.1.0</version>
	<packaging>pom</packaging>
	<name>Stacklight SDK</name>

	<!--
	  Built separately from the collector. The collector is a deployable that can
	  chase the newest Java; a library that other people's applications put on their
	  classpath cannot, so this targets 17 and inherits nothing from Spring Boot.
	-->
	<modules>
		<module>stacklight-client</module>
		<module>stacklight-spring</module>
	</modules>

	<properties>
		<maven.compiler.release>17</maven.compiler.release>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<junit.version>5.14.1</junit.version>
		<assertj.version>3.27.7</assertj.version>
		<spring.boot.version>4.1.0</spring.boot.version>
	</properties>

	<dependencyManagement>
		<dependencies>
			<dependency>
				<groupId>org.junit</groupId>
				<artifactId>junit-bom</artifactId>
				<version>${junit.version}</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>
			<dependency>
				<groupId>org.assertj</groupId>
				<artifactId>assertj-core</artifactId>
				<version>${assertj.version}</version>
				<scope>test</scope>
			</dependency>
		</dependencies>
	</dependencyManagement>

	<build>
		<pluginManagement>
			<plugins>
				<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-surefire-plugin</artifactId>
					<version>3.5.6</version>
				</plugin>
			</plugins>
		</pluginManagement>
	</build>
</project>
